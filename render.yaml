services:
  - type: web
    name: systemtwin
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: node apps/server/dist/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        sync: false
